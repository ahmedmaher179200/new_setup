<div class="form-group clearfix">
    <div class="">
        <input type="checkbox" id="{{$tag}}" name="{{$name}}" value="{{$value}}" {{$attribute}}>
        <label for="{{$tag}}">
        {{$label}}
        </label>
    </div>
</div>